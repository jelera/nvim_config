--[[
Solargraph Language Server Configuration
=========================================

Custom configuration for solargraph (Ruby Language Server).

Returns a table that gets merged with default LSP settings.
--]]

return {
  -- Use defaults - solargraph works well out of the box
  settings = {},
}
