--[[
Bash Language Server Configuration
===================================

Custom configuration for bashls (Bash Language Server).

Returns a table that gets merged with default LSP settings.
--]]

return {
  -- Use defaults - bashls works well out of the box
  settings = {},
}
