--[[
PostgreSQL Language Server Configuration
=========================================

Custom configuration for postgres_lsp (PostgreSQL Language Server).

Returns a table that gets merged with default LSP settings.
--]]

return {
	-- Use defaults
	settings = {},
}
