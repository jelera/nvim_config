--[[
Rust Analyzer Configuration
============================

Custom configuration for rust_analyzer (Rust Language Server).

Returns a table that gets merged with default LSP settings.
--]]

return {
	-- Use defaults - rust_analyzer has excellent defaults
	settings = {},
}
