--[[
SQL Language Server Configuration
==================================

Custom configuration for sqlls (SQL Language Server).

Returns a table that gets merged with default LSP settings.
--]]

return {
  -- Use defaults
  settings = {},
}
